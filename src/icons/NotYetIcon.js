/*import React, { useState } from 'react';
import { TodoIcon } from '../icons/TodoIcon';

function NotYetIcon({  }) {
  console.log('onNotYet:', onNotYet);

  const [mostrarNotYet, setMostrarNotYet] = useState(true);

  const handleNotYetClick = () => {
    console.log('Haciendo clic en NotYetIcon');
    setMostrarNotYet(true);
   // onNotYet();  // Llama a la función proporcionada desde las props para manejar cualquier lógica adicional
  };

  return (
    mostrarNotYet && (
      <TodoIcon
        type="notYet"
        color="gray"
        onClick={handleNotYetClick}
      />
    )
  );
}


export { NotYetIcon };
*/